# Google Analytics 4 measurement ID for nikitauday.github.io.
# Paste the "G-" ID from GA4 Admin > Data streams > your web stream.
# This value ships in the public JS bundle by design — it is not a secret, so it
# is committed here for the GitHub Pages build to pick up.
VITE_GA_ID=G-HVBWT3E3FY
